import {ICase} from '../desk/resources/case';
import {Reducer} from 'redux';
import * as Immutable from 'immutable';
/*
export const filters: Reducer = (state: ICase[]) => {
  switch( action.type ) {
    case 
  }
}

export const cases: Reducer = (state: ICase[]) => {
  switch( action.type ) {
    case 
  }
}

export const filter: Immutable.Map<number, 

Immutable.Record({a:'a'})*/